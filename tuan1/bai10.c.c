#include<stdio.h>
#include<math.h>
int main()
{
		int num;
		printf("Nhap X:");
		scanf("%d",&num);
			if(num%2==0){printf("Day la so chan");
			}	
		
			else{
			printf("Day la so le");
			return 0;
			}
}
